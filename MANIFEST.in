# sdist contents.
#
# setuptools sweeps tests/test*.py into the sdist by default. The published
# artifacts carry only the oubliette_trap package plus metadata; the test suite
# stays in the repo. Enforced by tests/test_packaging_boundary.py.
prune tests
